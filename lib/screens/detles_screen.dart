import 'package:flutter/material.dart';

class DetlesScreen extends StatelessWidget {
  const DetlesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(backgroundColor: Colors.black,),
    );
  }
}
