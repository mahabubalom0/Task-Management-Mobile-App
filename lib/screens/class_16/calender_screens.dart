import 'package:flutter/material.dart';

class CalenderScreens extends StatelessWidget {
  const CalenderScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.blue,
    );
  }
}
